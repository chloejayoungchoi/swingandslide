import React from "react";

function Loading(p) {

    return (
        <div className="loading-container">
            <img src="images/loading.gif" style={{width:'30vw'}}/>
        </div>
    );
}

export default Loading;